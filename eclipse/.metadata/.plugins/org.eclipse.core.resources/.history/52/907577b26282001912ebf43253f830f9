package com.setrion.koratio.events;

import com.setrion.koratio.blocks.ModBlocks;
import com.setrion.koratio.main.Koratio;
import com.setrion.koratio.network.MyMessage;
import com.setrion.koratio.network.PacketHandler;
import com.setrion.koratio.proxy.ClientProxy;

import net.minecraft.client.Minecraft;
import net.minecraft.client.renderer.texture.TextureMap;
import net.minecraft.util.ResourceLocation;
import net.minecraftforge.client.event.TextureStitchEvent;
import net.minecraftforge.fml.common.eventhandler.SubscribeEvent;
import net.minecraftforge.fml.common.gameevent.TickEvent;
import net.minecraftforge.fml.common.gameevent.TickEvent.ClientTickEvent;

@net.minecraftforge.fml.common.Mod.EventBusSubscriber(modid="koratio", value={net.minecraftforge.fml.relauncher.Side.CLIENT})
public class ClientEvents {
	
	public ClientEvents() {}
	
	@SubscribeEvent
	public static void renderTick(TickEvent.RenderTickEvent event) {
		ModBlocks.blockJubanLeaves.setGraphicsLevel(Minecraft.getMinecraft().gameSettings.fancyGraphics);
        ModBlocks.blockFlamingLeaves.setGraphicsLevel(Minecraft.getMinecraft().gameSettings.fancyGraphics);
        ModBlocks.blockVaresoLeaves.setGraphicsLevel(Minecraft.getMinecraft().gameSettings.fancyGraphics);
	}
	
	@SubscribeEvent
	public static void texStitch(TextureStitchEvent.Pre event) {
		TextureMap map = event.getMap();

		map.registerSprite(new ResourceLocation(Koratio.modid, "particle/glint"));
	}
	
	@SubscribeEvent
	public static void onKeyPress(ClientTickEvent event) {
		if (ClientProxy.KeySpawnSoul.isPressed()) {
			PacketHandler.INSTANCE.sendToServer(new MyMessage());
		}
	}
	
}
